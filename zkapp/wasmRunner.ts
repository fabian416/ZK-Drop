export const generateZKProof = async (lat: number, lon: number) => {
    // Dummy response: replace with WASM
    console.log('Running fake proof for:', lat, lon);
    return { proof: 'dummy-proof' };
  };